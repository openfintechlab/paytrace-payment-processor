[project]
name = "paytrace-payment-processor"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "environs",
  "jsonschema>=4.23.0",
  "pika",
  "python-dotenv",
  "sqlalchemy",
  "psycopg2-binary",
  "pytest",
]
